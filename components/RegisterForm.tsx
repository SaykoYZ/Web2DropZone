"use client";
import { useState } from "react"; import { useRouter } from "next/navigation";
export function RegisterForm(){const [email,setEmail]=useState(""),[password,setPassword]=useState(""),[confirm,setConfirm]=useState(""),[error,setError]=useState(""),router=useRouter();async function submit(e:React.FormEvent){e.preventDefault();const r=await fetch("/api/auth/register",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password,confirmPassword:confirm})});const d=await r.json();if(!r.ok){setError(d.error||"Erreur");return}router.push("/dashboard")}return <form onSubmit={submit} className="metal w-full max-w-md rounded-3xl p-8"><a href="/" className="text-sm text-white/40">← DROPZONE</a><h1 className="mt-8 text-3xl font-black">Créer un compte</h1><div className="mt-8 space-y-4"><input value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email" type="email" className="w-full rounded-xl border border-white/10 bg-white/[.04] px-4 py-3"/><input value={password} onChange={e=>setPassword(e.target.value)} placeholder="Mot de passe (8 caractères min.)" type="password" className="w-full rounded-xl border border-white/10 bg-white/[.04] px-4 py-3"/><input value={confirm} onChange={e=>setConfirm(e.target.value)} placeholder="Confirmation" type="password" className="w-full rounded-xl border border-white/10 bg-white/[.04] px-4 py-3"/>{error&&<div className="text-sm text-red-300">{error}</div>}<button className="w-full rounded-xl bg-white py-3 font-bold text-black">Créer mon compte</button></div></form>}