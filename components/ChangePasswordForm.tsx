"use client";
import {useState} from "react";import {useRouter} from "next/navigation";
export function ChangePasswordForm({email}:{email:string}){const[p,setP]=useState(""),[c,setC]=useState(""),[error,setError]=useState(""),[loading,setLoading]=useState(false),router=useRouter();
async function submit(e:React.FormEvent){e.preventDefault();setError("");setLoading(true);const r=await fetch("/api/auth/change-password",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({password:p,confirmPassword:c})});const d=await r.json();if(!r.ok){setError(d.error||"Erreur.");setLoading(false);return}router.replace("/admin");router.refresh()}
return <form onSubmit={submit} className="dz-metal w-full max-w-lg rounded-[32px] p-8 md:p-10"><div className="text-[10px] font-bold tracking-[.5em] text-white/30">INITIAL SETUP / ADMIN</div><h1 className="mt-3 text-4xl font-black tracking-tight">Sécurise ton accès.</h1><p className="mt-3 text-sm leading-6 text-white/40">Le compte <span className="text-white/70">{email}</span> utilise encore son mot de passe initial.</p><div className="mt-8 space-y-4"><label className="block"><span className="mb-2 block text-xs text-white/40">NOUVEAU MOT DE PASSE</span><input required minLength={8} value={p} onChange={e=>setP(e.target.value)} type="password" className="dz-input w-full rounded-xl px-4 py-3"/></label><label className="block"><span className="mb-2 block text-xs text-white/40">CONFIRMATION</span><input required minLength={8} value={c} onChange={e=>setC(e.target.value)} type="password" className="dz-input w-full rounded-xl px-4 py-3"/></label>{error&&<div className="rounded-xl border border-red-400/20 bg-red-400/5 px-4 py-3 text-sm text-red-200">{error}</div>}<button disabled={loading} className="dz-button w-full rounded-xl bg-white py-3.5 font-black text-black">{loading?"ENREGISTREMENT...":"ENREGISTRER ET ENTRER →"}</button></div></form>}