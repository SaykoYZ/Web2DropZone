"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
export function LoginForm() {
  const [email,setEmail]=useState(""); const [password,setPassword]=useState(""); const [error,setError]=useState(""); const [loading,setLoading]=useState(false); const router=useRouter();
  async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError("");const r=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password})});const d=await r.json();if(!r.ok){setError(d.error||"Erreur");setLoading(false);return}router.push(d.mustChangePassword?"/change-password":d.role==="ADMIN"||d.role==="SUPER_ADMIN"?"/admin":"/dashboard");}
  return <form onSubmit={submit} className="metal w-full max-w-md rounded-3xl p-8"><a href="/" className="text-sm text-white/40">← DROPZONE</a><h1 className="mt-8 text-3xl font-black">Connexion</h1><p className="mt-2 text-sm text-white/40">Accédez à votre espace.</p><div className="mt-8 space-y-4"><input value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email" type="email" className="w-full rounded-xl border border-white/10 bg-white/[.04] px-4 py-3 outline-none focus:border-white/30"/><input value={password} onChange={e=>setPassword(e.target.value)} placeholder="Mot de passe" type="password" className="w-full rounded-xl border border-white/10 bg-white/[.04] px-4 py-3 outline-none focus:border-white/30"/>{error&&<div className="text-sm text-red-300">{error}</div>}<button disabled={loading} className="w-full rounded-xl bg-white py-3 font-bold text-black disabled:opacity-50">{loading?"Connexion...":"Se connecter"}</button></div><p className="mt-6 text-center text-sm text-white/40">Pas de compte ? <a className="text-white" href="/register">Créer un compte</a></p></form>;
}