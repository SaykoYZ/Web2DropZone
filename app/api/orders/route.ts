import { NextResponse } from "next/server"; import { getCurrentUser } from "@/lib/server/session"; import { JsonOrderRepository } from "@/lib/repositories/orders"; export async function GET(){const u=await getCurrentUser();if(!u)return NextResponse.json({error:"Unauthorized"},{status:401});const all=await new JsonOrderRepository().getAll();return NextResponse.json(all.filter(o=>o.userId===u.id))}