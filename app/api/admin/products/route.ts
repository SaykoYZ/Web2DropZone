import {NextResponse} from "next/server";import crypto from "crypto";import {getCurrentUser} from "@/lib/server/session";import {JsonProductRepository} from "@/lib/repositories/products";import {addLog} from "@/lib/repositories/logs";
async function guard(){const u=await getCurrentUser();return u&&["ADMIN","SUPER_ADMIN"].includes(u.role)?u:null}
export async function POST(req:Request){const u=await guard();if(!u)return NextResponse.json({error:"Forbidden"},{status:403});const b=await req.json();if(!b.name||!b.slug)return NextResponse.json({error:"Nom et slug requis."},{status:400});const now=new Date().toISOString();const p=await new JsonProductRepository().create({id:crypto.randomUUID(),name:String(b.name),slug:String(b.slug),description:String(b.description||""),shortDescription:String(b.shortDescription||""),price:Number(b.price||0),currency:String(b.currency||"€"),image:String(b.image||""),images:Array.isArray(b.images)?b.images:[],category:String(b.category||"Autre"),badge:b.badge?String(b.badge):undefined,stock:Number(b.stock||0),active:b.active!==false,featured:!!b.featured,createdAt:now,updatedAt:now});await addLog({id:crypto.randomUUID(),type:"PRODUCT_CREATED",userId:u.id,description:`Produit créé : ${p.name}`,createdAt:now});return NextResponse.json(p,{status:201})}
export async function PATCH(req:Request){const u=await guard();if(!u)return NextResponse.json({error:"Forbidden"},{status:403});const b=await req.json();if(!b.id)return NextResponse.json({error:"ID requis."},{status:400});const allowed=["name","slug","description","shortDescription","price","currency","image","images","category","badge","stock","active","featured"];const data:any={};for(const k of allowed)if(k in b)data[k]=b[k];const p=await new JsonProductRepository().update(b.id,data);if(!p)return NextResponse.json({error:"Produit introuvable."},{status:404});await addLog({id:crypto.randomUUID(),type:"PRODUCT_UPDATED",userId:u.id,description:`Produit modifié : ${p.name}`,createdAt:new Date().toISOString()});return NextResponse.json(p)}
export async function DELETE(req:Request){const u=await guard();if(!u)return NextResponse.json({error:"Forbidden"},{status:403});const id=new URL(req.url).searchParams.get("id");if(!id)return NextResponse.json({error:"ID requis."},{status:400});const ok=await new JsonProductRepository().delete(id);if(!ok)return NextResponse.json({error:"Produit introuvable."},{status:404});await addLog({id:crypto.randomUUID(),type:"PRODUCT_DELETED",userId:u.id,description:`Produit supprimé : ${id}`,createdAt:new Date().toISOString()});return NextResponse.json({ok:true})}
