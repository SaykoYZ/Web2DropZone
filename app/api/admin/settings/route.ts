import { NextResponse } from "next/server"; import { getCurrentUser } from "@/lib/server/session"; import { updateSettings } from "@/lib/repositories/settings"; export async function GET(){const u=await getCurrentUser();if(!u||!["ADMIN","SUPER_ADMIN"].includes(u.role))return NextResponse.json({error:"Forbidden"},{status:403});return NextResponse.json(await import("@/lib/repositories/settings").then(x=>x.getSettings()))} export async function PATCH(req:Request){const u=await getCurrentUser();if(!u||!["ADMIN","SUPER_ADMIN"].includes(u.role))return NextResponse.json({error:"Forbidden"},{status:403});return NextResponse.json(await updateSettings(await req.json()))}