import { redirect } from "next/navigation";
import { getCurrentUser } from "@/lib/server/session";
import { ClientSidebar } from "@/components/layout/ClientSidebar";
export default async function Dashboard(){const user=await getCurrentUser();if(!user)redirect("/login");return <div className="flex min-h-screen bg-[#070707]"><ClientSidebar/><main className="flex-1 p-6 md:p-10"><div className="mb-10"><div className="text-xs tracking-[.35em] text-white/35">DASHBOARD</div><h1 className="mt-2 text-4xl font-black">Bienvenue, {user.displayName}</h1><p className="mt-2 text-white/45">{user.email}</p></div><div className="grid gap-4 md:grid-cols-4">{["Commandes","Terminées","En attente","Activité"].map((x,i)=><div key={x} className="metal rounded-2xl p-6"><div className="text-sm text-white/40">{x}</div><div className="mt-5 text-3xl font-black">{i===0?"0":"—"}</div></div>)}</div><div className="metal mt-6 rounded-3xl p-7"><h2 className="text-xl font-bold">Activité récente</h2><p className="mt-3 text-sm text-white/40">Aucune activité récente.</p></div></main></div>}