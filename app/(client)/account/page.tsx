import { redirect } from "next/navigation"; import { getCurrentUser } from "@/lib/server/session"; import { ClientSidebar } from "@/components/layout/ClientSidebar";
export default async function Account(){const u=await getCurrentUser();if(!u)redirect("/login");return <div className="flex min-h-screen"><ClientSidebar/><main className="flex-1 p-8"><h1 className="text-4xl font-black">Mon compte</h1><div className="metal mt-8 max-w-2xl rounded-3xl p-7 space-y-5"><div><div className="text-xs text-white/35">EMAIL</div><div className="mt-1">{u.email}</div></div><div><div className="text-xs text-white/35">NOM D'AFFICHAGE</div><div className="mt-1">{u.displayName}</div></div><div><div className="text-xs text-white/35">CRÉÉ LE</div><div className="mt-1">{new Date(u.createdAt).toLocaleDateString("fr-FR")}</div></div><div><div className="text-xs text-white/35">RÔLE</div><div className="mt-1">{u.role}</div></div></div></main></div>}