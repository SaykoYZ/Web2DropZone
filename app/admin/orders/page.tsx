import {JsonOrderRepository} from "@/lib/repositories/orders";import {JsonProductRepository} from "@/lib/repositories/products";import {JsonUserRepository} from "@/lib/repositories/users";import {OrderManager} from "@/components/admin/AdminClient";
export default async function AdminOrders(){const[orders,products,users]=await Promise.all([new JsonOrderRepository().getAll(),new JsonProductRepository().getAll(),new JsonUserRepository().getAll()]);return <main className="dz-grid min-h-screen p-6 md:p-10"><div className="mb-8"><div className="text-[10px] tracking-[.5em] text-white/30">COMMERCE / FULFILLMENT</div><h1 className="mt-3 text-4xl font-black md:text-5xl">Commandes</h1><p className="mt-2 text-sm text-white/35">Suis chaque commande et change son statut.</p></div><OrderManager initial={orders} products={products} users={users}/></main>}
