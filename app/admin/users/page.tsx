import {JsonUserRepository} from "@/lib/repositories/users";import {getCurrentUser} from "@/lib/server/session";import {UserManager} from "@/components/admin/AdminClient";
export default async function AdminUsers(){const[users,current]=await Promise.all([new JsonUserRepository().getAll(),getCurrentUser()]);return <main className="dz-grid min-h-screen p-6 md:p-10"><div className="mb-8"><div className="text-[10px] tracking-[.5em] text-white/30">PEOPLE / ACCESS</div><h1 className="mt-3 text-4xl font-black md:text-5xl">Utilisateurs</h1><p className="mt-2 text-sm text-white/35">Rôles, activation, suppression et réinitialisation des mots de passe.</p></div><UserManager initial={users} currentUserId={current!.id}/></main>}
