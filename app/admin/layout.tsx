import {redirect} from "next/navigation";import {getCurrentUser} from "@/lib/server/session";import {AdminSidebar} from "@/components/layout/AdminSidebar";import {Logo} from "@/components/Logo";import {getSettings} from "@/lib/repositories/settings";
export default async function AdminLayout({children}:{children:React.ReactNode}){const u=await getCurrentUser();if(!u||!["ADMIN","SUPER_ADMIN"].includes(u.role))redirect("/login");if(u.mustChangePassword)redirect("/change-password");const settings=await getSettings();return <div className="min-h-screen bg-[#070707]"><div className="border-b border-white/10 bg-black/70 p-4 backdrop-blur-xl lg:hidden"><Logo admin src={settings.logoImage}/></div><div className="flex min-h-screen"><AdminSidebar logoSrc={settings.logoImage}/><main className="min-w-0 flex-1">{children}</main></div></div>}
