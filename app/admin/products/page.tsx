import {JsonProductRepository} from "@/lib/repositories/products";import {ProductManager} from "@/components/admin/AdminClient";
export default async function AdminProducts(){const products=await new JsonProductRepository().getAll();return <main className="dz-grid min-h-screen p-6 md:p-10"><div className="mb-8"><div className="text-[10px] tracking-[.5em] text-white/30">COMMERCE / CATALOGUE</div><h1 className="mt-3 text-4xl font-black md:text-5xl">Produits</h1><p className="mt-2 text-sm text-white/35">Crée, modifie, active et supprime tes produits en direct.</p></div><ProductManager initial={products}/></main>}
